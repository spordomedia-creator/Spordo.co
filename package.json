{
  "name": "spordo",
  "version": "1.0.0",
  "private": true,
  "description": "SPORDO — NYC sports field & court permit availability tracker, served as a Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
